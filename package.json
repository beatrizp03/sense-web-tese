{
  "name": "@scientisst/sense-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "format": "prettier --write .",
    "dev:desktop": "concurrently -k \"pnpm --filter sense-web dev\" \"wait-on http://localhost:3000 && pnpm --filter sense-desktop start:no-warnings\"",
    "build:desktop": "pnpm --filter sense-web build",
    "start:desktop": "concurrently -k \"pnpm --filter sense-web start\" \"wait-on http://localhost:3000 && pnpm --filter sense-desktop start:no-warnings\"",
    "setup": "pnpm config set ignore-scripts false --location project && pnpm install",
    "clean:all": "pnpm -w dlx rimraf node_modules **/node_modules pnpm-lock.yaml && pnpm -w store prune",
    "reinstall": "pnpm run clean:all && pnpm install",
    "doctor": "node -v && pnpm -v && pnpm -w why electron && pnpm --filter sense-desktop exec electron --version"
  },
  "devDependencies": {
    "@scientisst/eslint-config-next": "workspace:*",
    "@scientisst/prettier-config": "workspace:*",
    "concurrently": "^9.2.1",
    "turbo": "^1.7.3",
    "typescript": "^5.9.3",
    "wait-on": "^9.0.4"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@10.30.1",
  "dependencies": {
    "@scientisst/sense": "^2.0.2"
  }
}